import { APP, COPY, ROUTES } from "../constants/app.js";
import { escapeHtml, money, date, productCover, renderProductCard, statusLabel } from "./helpers.js";

function header(session) {
  const account = session ? `<a class="avatar-link" href="${ROUTES.settings}" data-link><span class="avatar">${escapeHtml((session.first_name || session.username || "U")[0].toUpperCase())}</span><span>${escapeHtml(session.first_name || session.username)}</span></a>` : `<a class="button button-secondary" href="${ROUTES.auth}" data-link>Sign in</a>`;
  return `<header class="site-header"><a class="wordmark" href="${ROUTES.home}" data-link><span class="seal"><span>DS</span></span><span><strong>${APP.name}</strong><small>Digital products marketplace</small></span></a><nav class="site-nav"><a href="${ROUTES.catalog}" data-link>Browse</a><a href="${ROUTES.catalog}" data-link>Categories</a>${session ? `<a href="${ROUTES.library}" data-link>Library</a>` : ""}${session && statefulSeller(session) ? `<a href="${ROUTES.seller}" data-link>Seller workspace</a>` : ""}</nav><div class="header-actions">${!session ? `<a class="text-link" href="${ROUTES.becomeSeller}" data-link>For creators</a>` : ""}${account}</div></header>`;
}

function statefulSeller(session) { return [APP.roles.seller, APP.roles.admin].includes(session?.user_type); }

function workspaceSidebar(active, admin = false) {
  const links = admin
    ? [["applications", "Seller applications", ROUTES.adminSellerApplications], ["settings", "Account settings", ROUTES.settings]]
    : [["overview", "Overview", ROUTES.seller], ["products", "Products", ROUTES.sellerProducts], ["payouts", "Payouts", ROUTES.sellerPayouts], ["settings", "Store settings", ROUTES.settings]];
  return `<aside class="workspace-sidebar"><div><span class="sidebar-label">${admin ? "Administration" : "Creator workspace"}</span><nav>${links.map(([key, label, href]) => `<a class="${active === key ? "active" : ""}" href="${href}" data-link><span class="nav-dot"></span>${label}</a>`).join("")}</nav></div><a class="sidebar-help" href="${ROUTES.settings}" data-link>Help & account <span>→</span></a></aside>`;
}

function workspaceShell({ session, active, content, admin = false }) {
  return `${header(session)}<div class="workspace-shell">${workspaceSidebar(active, admin)}<main class="workspace-main">${content}</main></div>`;
}

export function home({ session, products }) {
  return `${header(session)}<main class="page"><section class="hero"><div class="hero-copy"><span class="eyebrow">Trusted by creators · Loved by customers</span><h1>Discover digital work <em>worth keeping.</em></h1><p>Premium PDFs, templates, prompts, guides, and tools to help you learn, build, and grow.</p><div class="hero-actions"><a class="button button-primary" href="${ROUTES.catalog}" data-link>Browse products <span>→</span></a><a class="button button-ghost" href="#how-it-works">How it works <span>↘</span></a></div><div class="hero-trust"><span>✓ Quality you can trust</span><span>⚡ Instant access</span><span>⌁ Secure & private</span></div></div><div class="hero-visual" aria-hidden="true"><div class="visual-orbit orbit-one"></div><div class="visual-orbit orbit-two"></div><div class="visual-card visual-card-main"><span class="visual-badge">DS</span><strong>Creator toolkit</strong><small>Templates · Guides · Prompts</small><div class="visual-progress"><i></i></div></div><div class="visual-card visual-card-pdf"><span>PDF</span></div><div class="visual-card visual-card-template"><span>⌘</span><small>Template</small></div><div class="visual-spark">✦</div></div></section><section class="trust-strip"><span>Secure payments</span><span>Instant digital delivery</span><span>Verified creators</span><span>Buyer-first support</span></section><section class="section-block"><div class="section-heading"><div><span class="eyebrow">Curated for you</span><h2>Explore what’s useful</h2><p>Practical digital resources from independent creators.</p></div><a class="text-link" href="${ROUTES.catalog}" data-link>View all products <span>→</span></a></div><div class="product-grid">${products.slice(0, 4).map(renderProductCard).join("") || `<p class="muted">${COPY.emptyProducts}</p>`}</div></section><section class="principles" id="how-it-works"><div><span class="eyebrow">Why DigiSutra</span><h2>From discovery to download in minutes.</h2></div><div class="principle-list"><article><span>01</span><h3>Find your next tool</h3><p>Browse focused products built for real work and clear outcomes.</p></article><article><span>02</span><h3>Pay with confidence</h3><p>Complete checkout securely through Razorpay.</p></article><article><span>03</span><h3>Keep it close</h3><p>Your purchases stay organized and ready in your library.</p></article></div></section></main>${footer()}`;
}

export function catalog({ session, products, query = "", category = "all" }) {
  const categories = ["all", ...new Set(products.map((p) => p.category).filter(Boolean))];
  return `${header(session)}<main class="page"><section class="catalog-intro"><span class="eyebrow">The marketplace</span><h1>Browse useful work.</h1><p>Digital products for clearer thinking, better making, and the work between.</p></section><form class="catalog-toolbar" data-catalog-form><label class="search-field"><span>⌕</span><input name="q" value="${escapeHtml(query)}" placeholder="Search products" /></label><select name="category">${categories.map((item) => `<option value="${escapeHtml(item)}" ${item === category ? "selected" : ""}>${escapeHtml(item === "all" ? "All categories" : item)}</option>`).join("")}</select><button class="button button-secondary" type="submit">Search</button></form><section class="catalog-results"><div class="results-meta"><span>${products.length} products</span><span>Sorted by newest</span></div><div class="product-grid">${products.map(renderProductCard).join("") || `<div class="full-width">${COPY.emptyProducts}</div>`}</div></section></main>${footer()}`;
}

export function detail({ session, product }) {
  if (!product) return `${header(session)}<main class="page">${notFound("Product not found", "This product may have been unpublished or moved.")}</main>${footer()}`;
  return `${header(session)}<main class="page detail-page"><a class="back-link" href="${ROUTES.catalog}" data-link>← Back to marketplace</a><section class="detail-grid"><div>${productCover(product)}</div><div class="detail-copy"><span class="eyebrow">${escapeHtml(product.category || "Digital product")}</span><h1>${escapeHtml(product.title)}</h1><p class="detail-byline">By ${escapeHtml(product.owner_username || "Independent creator")}</p><div class="detail-price">${escapeHtml(money(product.currency, product.price))}</div><p>${escapeHtml(product.description || "This creator has not added a description yet.")}</p><button class="button button-primary button-wide" data-buy-product="${escapeHtml(product.uuid)}">${session ? "Add to your library" : "Sign in to purchase"} <span>→</span></button><div class="trust-row"><span>✓ Instant access</span><span>✓ Secure checkout</span><span>✓ Creator-supported</span></div></div></section><section class="detail-lower"><div><span class="eyebrow">About this product</span><h2>Made to be used.</h2><p>Digital work should earn its place in your workflow. This product is delivered to your library after a successful purchase.</p></div><div class="detail-faq"><div><strong>What do I get?</strong><span>Digital access after payment</span></div><div><strong>Can I get a refund?</strong><span>Refund requests are handled through your order</span></div></div></section></main>${footer()}`;
}

export function auth({ mode = "login" }) { return `<main class="auth-page"><a class="wordmark auth-wordmark" href="${ROUTES.home}" data-link><span class="seal"><span>DS</span></span><span><strong>${APP.name}</strong><small>Digital products marketplace</small></span></a><section class="auth-layout"><div class="auth-message"><span class="eyebrow">Welcome to DigiSutra</span><h1>Your home for premium digital work.</h1><p>${COPY.authBody}</p><div class="auth-benefits"><span>⌁ Instant access to purchases</span><span>◇ Secure payments and private delivery</span><span>✦ Thoughtful products from real creators</span></div></div><div class="auth-card"><div class="auth-card-heading"><span class="eyebrow">Your account</span><h2>${mode === "login" ? "Welcome back" : "Create your account"}</h2><p>${mode === "login" ? "Sign in to continue to your library." : "Start discovering useful digital products."}</p></div><div class="auth-tabs"><button class="tab ${mode === "login" ? "active" : ""}" data-auth-mode="login">Sign in</button><button class="tab ${mode === "signup" ? "active" : ""}" data-auth-mode="signup">Create account</button></div><form data-auth-form data-mode="${mode}">${mode === "signup" ? `<div class="form-row"><label>First name<input name="first_name" required /></label><label>Last name<input name="last_name" /></label></div>` : ""}<label>Username<input name="username" autocomplete="username" required /></label>${mode === "signup" ? `<label>Email<input name="email" type="email" required /></label>` : ""}<label>Password<input name="password" type="password" autocomplete="current-password" required /></label><p class="form-error" data-form-error></p><button class="button button-primary button-wide" type="submit">${mode === "login" ? "Sign in securely" : "Create customer account"} <span>→</span></button></form><p class="form-note">Public accounts begin as customers. Seller access is reviewed separately.</p></div></section></main>`; }

export function library({ session, purchases }) { return `${header(session)}<main class="page workspace-page"><div class="workspace-heading"><div><span class="eyebrow">Your workspace</span><h1>Library</h1><p>Your purchased work, kept close.</p></div></div><section class="library-list">${purchases.length ? purchases.map((item) => { const order = item.order || {}; const asset = (item.assets || []).find((candidate) => candidate.asset_status === "verified"); const access = (item.access_records || []).find((candidate) => candidate.access_status === "granted"); const ready = order.payment_status === "paid" && Boolean(asset && access); return `<article class="library-row"><div class="library-cover">DS</div><div class="library-info"><span class="eyebrow">${escapeHtml(order.product_title || "Digital product")}</span><h3>${escapeHtml(order.product_title || "Untitled product")}</h3><p>Purchased ${escapeHtml(date(order.created_on))}</p></div><span class="status status-${escapeHtml(order.delivery_status || "pending")}">${escapeHtml(statusLabel(order.delivery_status))}</span><button class="button button-secondary" ${ready ? `data-library-order="${escapeHtml(order.uuid)}" data-library-asset="${escapeHtml(asset.uuid)}"` : "disabled"}>${ready ? "Download" : "Not ready"}</button></article>`; }).join("") : `<div class="panel-card"><h3>Your library is waiting.</h3><p>Purchase a product and it will appear here with its access and order status.</p><a class="button button-primary" href="${ROUTES.catalog}" data-link>Browse products</a></div>`}</section></main>${footer()}`; }

export function seller({ session, summary, products }) { const s = summary || {}; const content = `<div class="workspace-heading compact"><div><span class="eyebrow">Creator workspace</span><h1>Overview</h1><p>Here’s what is happening with your store.</p></div><a class="button button-primary" href="${ROUTES.sellerProductNew}" data-link>Add product <span>+</span></a></div><section class="metric-grid">${[["Gross sales", money(APP.defaultCurrency, s.gross_sales_amount)], ["Net earnings", money(APP.defaultCurrency, s.net_seller_amount)], ["Available for payout", money(APP.defaultCurrency, s.available_for_payout)], ["Products", s.products_count ?? 0]].map(([label, value], index) => `<article class="metric-card"><span>${label}</span><strong>${escapeHtml(value)}</strong><small>${index < 3 ? "Updated from verified orders" : "In your catalogue"}</small></article>`).join("")}</section><section class="workspace-grid"><div class="panel-card"><div class="section-heading"><div><span class="eyebrow">Catalogue</span><h2>Your products</h2></div><a class="text-link" href="${ROUTES.sellerProducts}" data-link>View all →</a></div>${products.slice(0, 4).map((p) => `<div class="mini-row"><div>${productCover(p, true)}</div><div><strong>${escapeHtml(p.title)}</strong><span>${escapeHtml(p.category || "Digital product")}</span></div><span>${escapeHtml(money(p.currency, p.price))}</span></div>`).join("") || `<p class="muted">No products yet.</p>`}</div><div class="panel-card readiness-card"><span class="status-icon">${s.payout_ready && !s.payout_hold ? "✓" : "!"}</span><span class="eyebrow">Payout readiness</span><h2>${s.payout_hold ? "Payouts are on hold" : s.payout_ready ? "You’re ready to get paid" : "Complete your profile"}</h2><p>${s.payout_hold ? "An admin has placed a temporary hold on payouts." : s.payout_ready ? `${money(APP.defaultCurrency, s.available_for_payout)} is currently available for payout.` : "Finish KYC and fund-account validation before requesting payouts."}</p><a class="button button-secondary button-wide" href="${ROUTES.sellerPayouts}" data-link>Review payouts <span>→</span></a></div></section>`; return `${workspaceShell({ session, active: "overview", content })}${footer()}`; }

export function sellerProducts({ session, products }) { const content = `<div class="workspace-heading compact"><div><span class="eyebrow">Creator workspace</span><h1>Products</h1><p>Manage and monitor your digital products.</p></div><a class="button button-primary" href="${ROUTES.sellerProductNew}" data-link>Add product <span>+</span></a></div><div class="table-toolbar table-toolbar-summary"><strong>Your catalogue</strong><span>${products.length} total products</span></div><div class="table-card"><div class="table-head"><span>Product</span><span>Category</span><span>Price</span><span>Status</span></div>${products.map((p) => `<div class="table-row"><div class="table-product">${productCover(p, true)}<span><strong>${escapeHtml(p.title)}</strong><small>${escapeHtml(p.description || "No description")}</small></span></div><span>${escapeHtml(p.category || "—")}</span><span>${escapeHtml(money(p.currency, p.price))}</span><span class="status status-${p.is_active && p.is_public ? "ready" : "draft"}">${p.is_active && p.is_public ? "Published" : "Draft"}</span></div>`).join("") || `<p class="empty-table">No products yet.</p>`}</div>`; return `${workspaceShell({ session, active: "products", content })}${footer()}`; }

export function sellerProductNew({ session }) { const content = `<div class="workspace-heading compact"><div><span class="eyebrow">Product workflow</span><h1>Create product</h1><p>Add a new digital product to your store.</p></div></div><ol class="form-steps"><li class="active"><span>1</span><div><strong>Details</strong><small>Product information</small></div></li><li><span>2</span><div><strong>Pricing</strong><small>Price and currency</small></div></li><li><span>3</span><div><strong>Upload</strong><small>Private product file</small></div></li><li><span>4</span><div><strong>Publish</strong><small>Review and create</small></div></li></ol><div class="panel-card form-panel"><div class="form-panel-heading"><span class="eyebrow">Product details</span><h2>What are you selling?</h2><p>Customers will see this information before purchase.</p></div><form data-product-form><label>Product title<input name="title" placeholder="e.g. Product strategy toolkit" required /></label><label>Description<textarea name="description" rows="5" placeholder="Explain what the customer receives and how it helps."></textarea></label><div class="form-row"><label>Price<input name="price" inputmode="decimal" placeholder="499" required /></label><label>Currency<input name="currency" value="${APP.defaultCurrency}" /></label></div><label>Category<input name="category" placeholder="Templates, prompts, guides…" /></label><label class="file-drop"><span class="upload-icon">↑</span><strong>Upload product file</strong><small>Choose the file customers receive after payment</small><input name="asset_file" type="file" required /></label><p class="form-note info-note">Files upload directly to private storage and are verified before the product is ready.</p><p class="form-error" data-form-error></p><div class="form-actions"><a class="button button-secondary" href="${ROUTES.sellerProducts}" data-link>Cancel</a><button class="button button-primary" type="submit">Create and upload <span>→</span></button></div></form></div>`; return `${workspaceShell({ session, active: "products", content })}${footer()}`; }

export function sellerPayouts({ session, summary }) { const s = summary || {}; const paidTotal = (s.payouts || []).filter((p) => p.status === "paid").reduce((total, p) => total + Number(p.amount || 0), 0); const content = `<div class="workspace-heading compact"><div><span class="eyebrow">Creator workspace</span><h1>Payouts</h1><p>Track earnings and payout activity.</p></div></div><section class="metric-grid payout-metrics"><article class="metric-card metric-featured"><span>Available for payout</span><strong>${escapeHtml(money(s.currency, s.available_for_payout))}</strong><small>Ready to transfer</small></article><article class="metric-card"><span>Pending</span><strong>${escapeHtml(money(s.currency, s.pending_payout))}</strong><small>Processing or reserved</small></article><article class="metric-card"><span>Paid</span><strong>${escapeHtml(money(s.currency, paidTotal.toFixed(2)))}</strong><small>Successfully paid</small></article><article class="metric-card"><span>Readiness</span><strong>${s.payout_ready ? "Ready" : "Not ready"}</strong><small>${s.payout_hold ? "Payout hold is active" : "KYC and fund account"}</small></article></section><div class="payout-banner"><div><span class="status-icon">${s.payout_ready ? "✓" : "!"}</span><div><strong>${s.payout_ready ? "Your account is payout ready" : "Complete payout verification"}</strong><p>${s.payout_ready ? "Available funds can be requested through the configured payout process." : "KYC and fund-account validation are required before payout."}</p></div></div></div><div class="table-card"><div class="table-card-title"><div><span class="eyebrow">Transactions</span><h2>Payout history</h2></div></div><div class="table-head"><span>Created</span><span>Method</span><span>Amount</span><span>Status</span></div>${(s.payouts || []).map((p) => `<div class="table-row"><span>${escapeHtml(date(p.created_on))}</span><span>${escapeHtml(p.payout_method || "Manual")}</span><strong>${escapeHtml(money(s.currency, p.amount))}</strong><span class="status status-${escapeHtml(p.status)}">${escapeHtml(statusLabel(p.status))}</span></div>`).join("") || `<p class="empty-table">No payouts recorded yet.</p>`}</div>`; return `${workspaceShell({ session, active: "payouts", content })}${footer()}`; }

export function settings({ session }) {
  const adminCard = session.user_type === APP.roles.admin ? `<div class="panel-card"><h3>Seller requests</h3><p>Review pending seller applications and approve, reject, or request more information.</p><a class="button button-primary" href="${ROUTES.adminSellerApplications}" data-link>Review requests <span>→</span></a></div>` : "";
  return `${header(session)}<main class="page workspace-page account-page"><div class="workspace-heading compact"><div><span class="eyebrow">Account settings</span><h1>${escapeHtml(session.first_name || session.username || "Account")}</h1><p>Manage your profile, purchases, and seller access.</p></div><button class="button button-danger" type="button" data-logout>Log out</button></div><section class="account-grid"><aside class="account-nav"><a class="active" href="${ROUTES.settings}" data-link>Profile</a><a href="${ROUTES.library}" data-link>Purchases</a>${statefulSeller(session) ? `<a href="${ROUTES.seller}" data-link>Seller workspace</a>` : `<a href="${ROUTES.becomeSeller}" data-link>Seller access</a>`}</aside><div class="account-content"><div class="panel-card profile-card"><div class="profile-identity"><span class="profile-avatar">${escapeHtml((session.first_name || session.username || "U")[0].toUpperCase())}</span><div><span class="eyebrow">Profile information</span><h2>${escapeHtml(`${session.first_name || ""} ${session.last_name || ""}`.trim() || session.username)}</h2><p>${escapeHtml(session.email || session.username || "")}</p></div></div><span class="status status-ready">Account active</span></div><div class="account-cards"><div class="panel-card"><span class="card-icon">▣</span><h3>Your library</h3><p>Open purchased products and check delivery status.</p><a class="button button-secondary" href="${ROUTES.library}" data-link>View purchases <span>→</span></a></div><div class="panel-card accent-card"><span class="card-icon">◇</span><h3>${session.user_type === APP.roles.customer ? "Become a seller" : "Creator workspace"}</h3><p>${session.user_type === APP.roles.customer ? "Apply to publish products and manage payouts." : "Manage products, earnings, and payouts."}</p><a class="button button-primary" href="${session.user_type === APP.roles.customer ? ROUTES.becomeSeller : ROUTES.seller}" data-link>${session.user_type === APP.roles.customer ? "Get seller access" : "Open workspace"} <span>→</span></a></div>${adminCard}</div></div></section></main>${footer()}`;
}

export function sellerApplication({ session, application }) {
  const app = application || {};
  const locked = ["kyc_pending", "kyc_in_review", "kyc_verified", "approved"].includes(app.status);
  return `${header(session)}<main class="page workspace-page onboarding-page"><div class="workspace-heading compact"><div><span class="eyebrow">Seller onboarding</span><h1>Become a seller</h1><p>Tell us about your business and complete verification.</p></div><div class="status-stack"><span class="status status-${escapeHtml(app.status || "draft")}">${escapeHtml(statusLabel(app.status || "draft"))}</span><span class="status status-${escapeHtml(app.kyc_status || "not_started")}">KYC ${escapeHtml(statusLabel(app.kyc_status || "not started"))}</span></div></div><div class="onboarding-layout"><aside class="onboarding-steps"><span class="sidebar-label">Application progress</span><ol><li class="active"><span>1</span><div><strong>Store details</strong><small>Your creator identity</small></div></li><li><span>2</span><div><strong>Business identity</strong><small>Legal information</small></div></li><li><span>3</span><div><strong>Bank details</strong><small>Payout destination</small></div></li><li><span>4</span><div><strong>KYC documents</strong><small>Verification reference</small></div></li><li><span>5</span><div><strong>Review & submit</strong><small>Final confirmation</small></div></li></ol><div class="privacy-note"><strong>Your data is safe</strong><p>Seller details are used only for verification and payouts.</p></div></aside><div class="panel-card form-panel"><div class="form-panel-heading"><span class="eyebrow">Application details</span><h2>Build your seller profile</h2><p>Complete every required field before submitting for review.</p></div><form data-seller-application-form><div class="form-section-title"><span>01</span><div><strong>Store details</strong><small>How customers will recognize you</small></div></div><div class="form-row"><label>Store name<input name="store_name" value="${escapeHtml(app.store_name || "")}" required ${locked ? "disabled" : ""} /></label><label>Category<input name="category" value="${escapeHtml(app.category || "")}" required ${locked ? "disabled" : ""} /></label></div><label>Store description<textarea name="store_description" rows="4" required ${locked ? "disabled" : ""}>${escapeHtml(app.store_description || "")}</textarea></label><label>Product types<input name="product_types" value="${escapeHtml(app.product_types || "")}" placeholder="Templates, guides, prompts" required ${locked ? "disabled" : ""} /></label><div class="form-section-title"><span>02</span><div><strong>Business identity</strong><small>Legal and contact information</small></div></div><div class="form-row"><label>Legal name<input name="legal_name" value="${escapeHtml(app.legal_name || "")}" required ${locked ? "disabled" : ""} /></label><label>Business type<select name="business_type" required ${locked ? "disabled" : ""}>${["", "individual", "proprietorship", "partnership", "llp", "private_limited", "public_limited", "trust", "ngo", "society"].map((item) => `<option value="${item}" ${item === (app.business_type || "") ? "selected" : ""}>${escapeHtml(item ? statusLabel(item) : "Select business type")}</option>`).join("")}</select></label></div><label>Business address<textarea name="business_address" rows="3" required ${locked ? "disabled" : ""}>${escapeHtml(app.business_address || "")}</textarea></label><div class="form-row"><label>PAN<input name="pan_number" value="${escapeHtml(app.pan_number || "")}" maxlength="10" required ${locked ? "disabled" : ""} /></label><label>GSTIN<input name="gstin" value="${escapeHtml(app.gstin || "")}" maxlength="15" ${locked ? "disabled" : ""} /></label></div><div class="form-row"><label>Country<input name="country" value="${escapeHtml(app.country || "")}" required ${locked ? "disabled" : ""} /></label><label>Phone number<input name="phone_number" value="${escapeHtml(app.phone_number || "")}" required ${locked ? "disabled" : ""} /></label></div><div class="form-section-title"><span>03</span><div><strong>Payout and verification</strong><small>Bank and KYC information</small></div></div><div class="form-row"><label>Bank account holder<input name="bank_account_holder_name" value="${escapeHtml(app.bank_account_holder_name || "")}" required ${locked ? "disabled" : ""} /></label><label>Bank account last 4<input name="bank_account_last4" value="${escapeHtml(app.bank_account_last4 || "")}" maxlength="4" inputmode="numeric" required ${locked ? "disabled" : ""} /></label></div><label>Bank IFSC<input name="bank_ifsc" value="${escapeHtml(app.bank_ifsc || "")}" maxlength="11" required ${locked ? "disabled" : ""} /></label><div class="form-row"><label>KYC document type<input name="kyc_document_type" value="${escapeHtml(app.kyc_document_type || "")}" placeholder="PAN, GST, bank proof" required ${locked ? "disabled" : ""} /></label><label>KYC document reference<input name="kyc_document_reference" value="${escapeHtml(app.kyc_document_reference || "")}" placeholder="Private reference" required ${locked ? "disabled" : ""} /></label></div><div class="form-row"><label>Website<input name="website_url" value="${escapeHtml(app.website_url || "")}" ${locked ? "disabled" : ""} /></label><label>Portfolio URL<input name="portfolio_url" value="${escapeHtml(app.portfolio_url || "")}" ${locked ? "disabled" : ""} /></label></div><label class="check-row"><input type="checkbox" name="terms_accepted" ${app.terms_accepted ? "checked" : ""} ${locked ? "disabled" : ""} /> I accept the seller terms</label><p class="form-error" data-form-error>${escapeHtml(app.review_note || app.kyc_review_note || "")}</p>${locked ? `<div class="locked-note">This application is currently under review. We’ll show any required next step here.</div>` : `<div class="form-actions"><button class="button button-secondary" type="submit" data-seller-application-action="draft">Save draft</button><button class="button button-primary" type="submit" data-seller-application-action="submit">Submit for review <span>→</span></button></div>`}</form></div></div></main>${footer()}`;
}

export function adminSellerApplications({ session, applications }) {
  const rows = applications.map((item) => {
    const canStartKyc = ["submitted", "under_review", "kyc_pending"].includes(item.status);
    const canVerifyKyc = ["kyc_pending", "kyc_in_review", "kyc_failed"].includes(item.status);
    const canApprove = item.status === "kyc_verified";
    const canReject = ["submitted", "under_review", "kyc_pending", "kyc_in_review", "kyc_failed", "needs_information"].includes(item.status);
    return `<article class="admin-request-row"><div><span class="eyebrow">${escapeHtml(item.category || "Seller request")}</span><h3>${escapeHtml(item.store_name || "Unnamed store")}</h3><p>${escapeHtml(item.store_description || "No description provided.")}</p><small>${escapeHtml(item.applicant?.email || item.applicant?.username || "Unknown applicant")} · ${escapeHtml(date(item.submitted_on || item.created_on))}</small><small>KYC ${escapeHtml(statusLabel(item.kyc_status || "not_started"))} · Fund ${escapeHtml(statusLabel(item.fund_account_status || "not_started"))} · ${escapeHtml(item.provider || "manual")}</small></div><div class="status-stack"><span class="status status-${escapeHtml(item.status)}">${escapeHtml(statusLabel(item.status))}</span><span class="status status-${escapeHtml(item.kyc_status || "not_started")}">KYC ${escapeHtml(statusLabel(item.kyc_status || "not started"))}</span></div><div class="form-actions">${canStartKyc ? `<button class="button button-secondary" type="button" data-admin-seller-action="start-kyc-review" data-application-uuid="${escapeHtml(item.uuid)}">Start KYC</button>` : ""}${canVerifyKyc ? `<button class="button button-primary" type="button" data-admin-seller-action="verify-kyc" data-application-uuid="${escapeHtml(item.uuid)}">Verify KYC</button><button class="button button-secondary" type="button" data-admin-seller-action="fail-kyc" data-application-uuid="${escapeHtml(item.uuid)}">Fail KYC</button>` : ""}${canApprove ? `<button class="button button-primary" type="button" data-admin-seller-action="approve" data-application-uuid="${escapeHtml(item.uuid)}">Approve seller</button>` : ""}${canReject ? `<button class="button button-secondary" type="button" data-admin-seller-action="request-information" data-application-uuid="${escapeHtml(item.uuid)}">Request info</button><button class="button button-secondary" type="button" data-admin-seller-action="reject" data-application-uuid="${escapeHtml(item.uuid)}">Reject</button>` : ""}</div></article>`;
  }).join("");
  const content = `<div class="workspace-heading compact"><div><span class="eyebrow">Trust operations</span><h1>Seller applications</h1><p>Review verification status and seller readiness.</p></div><button class="button button-secondary" type="button" data-refresh-admin-sellers>Refresh</button></div><div class="table-toolbar table-toolbar-summary"><strong>Application queue</strong><span>${applications.length} applications</span></div><section class="admin-request-list">${rows || `<div class="panel-card"><h3>No seller requests</h3><p>Submitted seller applications will appear here.</p></div>`}</section>`;
  return `${workspaceShell({ session, active: "applications", content, admin: true })}${footer()}`;
}

export function checkout({ session, product, checkout }) {
  if (!product) return `${header(session)}<main class="page">${notFound("Product not found", "This product may have been unpublished or moved.")}</main>${footer()}`;
  const order = checkout?.order;
  return `${header(session)}<main class="page checkout-page"><a class="back-link" href="/products/${encodeURIComponent(product.uuid)}" data-link>← Back to product</a><section class="checkout-grid"><div class="panel-card"><span class="eyebrow">Checkout</span><h1>${escapeHtml(product.title)}</h1><p>${escapeHtml(product.description || "Digital access after payment confirmation.")}</p><div class="checkout-total"><span>Total</span><strong>${escapeHtml(money(product.currency, product.price))}</strong></div></div><div class="panel-card payment-panel"><span class="eyebrow">Payment</span>${order ? `<h2>Payment order ready</h2><p>Order ${escapeHtml(order.provider_order_id || order.uuid)} is ready for Razorpay checkout.</p><button class="button button-primary button-wide" data-open-razorpay>Pay with Razorpay <span>→</span></button><p class="form-note">Use Razorpay test card details in the popup. Access is added to your library after confirmation.</p>` : `<h2>Review and continue</h2><p>We will create your order and send you to Razorpay test checkout.</p><button class="button button-primary button-wide" data-start-checkout="${escapeHtml(product.uuid)}">Continue to payment <span>→</span></button>`}<p class="form-error" data-checkout-error></p></div></section></main>${footer()}`;
}

function footer() { return `<footer class="site-footer"><span>© ${new Date().getFullYear()} ${APP.name}</span><span>Thoughtful digital work, kept simple.</span></footer>`; }
function notFound(title, body) { return `<div class="not-found"><h1>${escapeHtml(title)}</h1><p>${escapeHtml(body)}</p><a class="button button-primary" href="${ROUTES.catalog}" data-link>Return to marketplace</a></div>`; }
